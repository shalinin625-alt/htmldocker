apiVersion: v1
kind: Service
metadata:
  name: factorial-service
spec:
  type: NodePort
  selector:
    app: factorial-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007